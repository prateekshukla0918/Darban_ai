// // src/utils/aiLogic.js

// // This function makes the AI select a random available move
// export function getEasyMove(board) {
//   // Get a list of all empty cells (denoted as null or "")
//   const emptyCells = board.filter(cell => cell === null);
  
//   // Select a random empty cell
//   const randomIndex = Math.floor(Math.random() * emptyCells.length);
//   return emptyCells[randomIndex];
// }

// // Optional: If you have other AI difficulty functions, you can export them similarly
// export function getMediumMove(board) {
//   // This can have a more advanced AI algorithm
// }

// export function getHardMove(board) {
//   // This would have the minimax algorithm or another smart strategy
// }
